# 计划：T7.7 CNB 代码与发行版同步

设计见 [设计-T7.7-CNB代码与发行版同步.md](./设计-T7.7-CNB代码与发行版同步.md)。
四个 Task 依次独立提交；Task 完成即删本计划（Git 历史追溯）。

约定：所有验证命令在 PowerShell 7、仓库根执行；`$env:GOCACHE = Join-Path $env:TEMP "auto-mas-runtime-verify"`。
每个「验证与提交」都要求 `git diff --check` 干净后再提交。

## Task 1 静态契约测试（红灯先行）

**文件**

- 修改 `internal/release/workflow_test.go`：新增
  - `TestSyncCnbWorkflow_MirrorContract`
  - `TestReleaseWorkflow_PublishCnbJobContract`
  - `TestBackfillWorkflow_ManualOnlyContract`
  - `TestPublishCnbScript_SafetyContract`
  - `readWorkflowSource(t, name)`（从 `releaseWorkflowSource` 提取的通用读取器）
- 新增 `internal/release/powershell_test.go`：
  - `TestWorkflowAndScripts_PowerShellAST`（提取全部 workflow `run` block 到临时文件 + `scripts/*.ps1`，调 `pwsh` Parser.ParseFile，parse error 即失败；非 Windows `t.Skip`）

**红灯**：以上测试引用尚不存在的 `.github/workflows/sync-cnb.yml`、`cnb-release-backfill.yml`、`scripts/publish-cnb-release.ps1` 及 release.yml 的 `publish-cnb` job，必然失败；先确认失败原因是「文件缺失/片段缺失」而非测试语法错误。

**验证**

```powershell
& go test ./internal/release -run "TestSyncCnb|TestReleaseWorkflow_PublishCnb|TestBackfill|TestPublishCnbScript|TestWorkflowAndScripts" -count=1
if ($LASTEXITCODE -eq 0) { throw "expected red run to fail" }
```

同时确认输出包含 `--- FAIL:` 且不含 `[no tests to run]`。

## Task 2 sync-cnb.yml 与 backfill.yml（绿灯之一）

**文件**

- 新增 `.github/workflows/sync-cnb.yml`（设计 5.1 节）
- 新增 `.github/workflows/cnb-release-backfill.yml`（设计 5.4 节；脚本步骤在 Task 4 落地，本任务先写 workflow 骨架，调用 `scripts/publish-cnb-release.ps1` 的行保留——该文件在 Task 4 提交，两个提交合起来自洽；本地静态测试只断言 workflow 文本）

**绿灯**：Task 1 中 `TestSyncCnbWorkflow_MirrorContract`、`TestBackfillWorkflow_ManualOnlyContract`、`TestWorkflowAndScripts_PowerShellAST` 的 workflow 部分。

**验证与提交**

```powershell
& go test ./internal/release -run "TestSyncCnb|TestBackfill|TestWorkflowAndScripts" -count=1
if ($LASTEXITCODE -ne 0) { throw "sync/backfill contract tests failed" }
```

提交：`feat: 新增 CNB 代码镜像与历史回填工作流 (T7.7)`

## Task 3 release.yml 增加 publish-cnb job（绿灯之二）

**文件**

- 修改 `.github/workflows/release.yml`：追加 `publish-cnb` job（设计 5.3 节），引用 `scripts/publish-cnb-release.ps1`

**绿灯**：`TestReleaseWorkflow_PublishCnbJobContract`；既有全部 `TestReleaseWorkflow_*` 必须保持绿（特别是 `contents: write` 计数仍为 1）。

**验证与提交**

```powershell
& go test ./internal/release -count=1
if ($LASTEXITCODE -ne 0) { throw "release contract tests failed" }
```

提交：`feat: Release 工作流追加 CNB 发布 job (T7.7)`

## Task 4 publish-cnb-release.ps1 与集成测试（绿灯之三）

**文件**

- 新增 `scripts/publish-cnb-release.ps1`（设计 5.2 节，约 400 行）
- 新增 `internal/release/publishcnb_test.go`：`TestPublishCnbScript_EndToEnd`（httptest 模拟 CNB API + 公开下载；`testCases` 表驱动覆盖设计 5.5 节全部 9 个场景；非 Windows `t.Skip`）

**红灯**：`TestPublishCnbScript_EndToEnd` 在脚本存在前失败。

**验证与提交**

```powershell
& go test ./internal/release -count=1
& go test ./internal/release -count=100
if ($LASTEXITCODE -ne 0) { throw "publish-cnb tests failed" }
```

提交：`feat: 实现 CNB Release 发布脚本与集成测试 (T7.7)`

## Task 5 收尾：标准门 + 文档回写

**文件**

- 修改 `doc/任务拆分.md`：M7 下登记 T7.7（勾选 + 证据），变更记录加一行；
- 修改 `AGENTS.md`：M7 状态行追加 T7.7。

**验证（标准门全绿后提交）**

```powershell
gofmt -l .            # 无输出
go vet ./...
go build -buildvcs=false ./...
go test ./... -count=1
git diff --check
```

提交：`docs: 登记 T7.7 CNB 同步完成情况`

## 远端验收（需单独授权，不在本地 Task 内）

1. 用户配置 `GIT_PASSWORD`、`CNB_TOKEN` secrets；
2. 授权 push 后，`sync-cnb.yml` 灌入 refs；`git ls-remote` 双侧比对无 diff；
3. dispatch backfill `tag=v0.1.10` 试点；
4. dispatch backfill `tag=all` 补齐 17 个；
5. 下个新版本 Tag 走自动双发布链路。
