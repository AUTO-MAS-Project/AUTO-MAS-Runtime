# T4.9 swap 前目录占用后的确定性恢复实施计划

## Task 1：冻结现场红灯

- 文件：`internal/gitrepo/recovery_test.go`
- 测试：`TestRecovery_SwapBeforeFirstRenameDamagedRepositoryDiscardsCandidate`
- 红灯：stage=swap 下损坏 repo 在分类阶段返回 `UPDATE_STATE_AMBIGUOUS` / `repository_unknown`。
- 绿灯：恢复保留 repo，删除目标 update 与 transaction，且不写 environment。

## Task 2：最小恢复实现

- 文件：`internal/gitrepo/recovery.go`
- 改动：把普通损坏 repo 的 incomplete 分类扩展到 swap 阶段；从 blanket incomplete 拒绝中排除
  repo，并让 repo valid/incomplete 共享「目标 update + previous 缺失」的 swap 前清理分支。
- 边界：update/previous incomplete 仍立即失败；其他 repo incomplete 形状落入既有 `swap_shape`
  fail-closed 分支。

## Task 3：回归与验收

- 定向测试重复 20 次；
- `go test ./internal/gitrepo -run '^TestRecovery|^TestComponent_GitReplacementMatrix' -count=10`；
- 执行 gofmt、go vet、go build、全仓 go test、`git diff --check`；
- 提升 GCC 目录后执行 `go test -race ./... -count=1`；
- 完成后删除本计划，设计移入 `doc/archive/M4/`，回写 T4.9 完成状态和验证证据。
