# T4.10 提交后旧仓库部分清理的实施计划

## Task 1：冻结提交后恢复边界

- 文件：`doc/架构设计.md`、`doc/任务拆分.md`、`doc/current/README.md`、本设计与计划。
- 验证：文档明确唯一可恢复形状、目录 token 约束和失败关闭反例；`git diff --check` 通过。
- 提交：`docs: 定稿提交后旧仓库清理恢复边界 (T4.10)`。

## Task 2：现场形态红灯

- 文件：`internal/gitrepo/recovery_test.go`。
- 测试：`TestRecovery_CleanupIncompletePreviousCompletes` 创建精确目标 active repo、缺失 update、
  `.git/HEAD` 与 `.git/config` 已缺失的 previous、匹配的 `repository_changed` 环境和 cleanup transaction；
  断言恢复成功、previous 与 transaction 被清理、active repo 不变、环境不重写。
- 反例：`TestRecovery_IncompletePreviousOutsideCommittedCleanupHasNoSideEffects` 覆盖 swap 阶段、缺少
  target Commit、update 存在和 active repo Commit 不匹配。
- 红灯：定向执行并确认主用例因 `previous_unknown` 失败，反例保持通过；拒绝 `[no tests to run]`。

## Task 3：最小恢复实现

- 文件：`internal/gitrepo/recovery.go`。
- 实现：cleanup 阶段允许 previous 保留目录 token 并分类为 incomplete；仅在 target Commit 非空、
  active repo 精确匹配且 update 缺失时进入既有 `completeActiveTarget`。其他 incomplete 形状仍 ambiguous。
- 绿灯：两项定向测试 `-count=20`，既有 `TestRecovery` 测试全绿。
- 提交：`fix: 恢复部分清理的旧仓库残留 (T4.10)`。

## Task 4：验收与归档

- 验证：Git Recovery/Replacement 组件矩阵、`gofmt`、`go vet`、`go build`、全仓测试、
  `git diff --check`、全仓 race detector；每条原生命令检查 `$LASTEXITCODE`。
- 审查：核对路径固定、token 复验、环境先写后删、提交点和错误码均未改变。
- 回写：任务标记完成，设计移入 `doc/archive/M4/`，删除实施计划，记录提交和验证证据。
- 提交：`docs: 记录 T4.10 提交后恢复完成情况`。
