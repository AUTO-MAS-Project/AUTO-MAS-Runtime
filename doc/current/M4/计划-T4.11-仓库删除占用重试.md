# T4.11 仓库删除占用重试计划

1. 在 `internal/filesystem/delete_windows_test.go` 增加一个三次瞬时失败后成功的红灯测试。
2. 在 `internal/filesystem/delete_windows.go` 实现 10 秒间隔、最多 3 次的可取消重试。
3. 把 `internal/gitrepo` 的四处独立清理预算统一延长到 60 秒。
4. 运行定向测试和标准验证门；未提交前保持任务为进行中。
