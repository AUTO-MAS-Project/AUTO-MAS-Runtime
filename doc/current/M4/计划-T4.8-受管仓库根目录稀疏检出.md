# T4.8 受管仓库根目录稀疏检出实施计划

## Task 1：冻结排除策略

- 文件：`internal/gitrepo/checkout_policy_test.go`、`internal/gitrepo/checkout_policy.go`；
- 红灯：`TestCheckoutPolicy_DefaultExclusions`、`TestCheckoutPolicy_RejectsInvalidExclusions`、
  `TestCheckoutPolicy_RootEntries` 因策略类型和构造器缺失而编译失败；
- 绿灯：集中返回 `frontend`、`.debug`，校验根条目与必需路径，按目标 tree 生成无歧义模式；
- 验证：定向测试出现 `--- PASS:`，不得包含 `[no tests to run]`；提交策略和测试。

## Task 2：接入 NoCheckout 与稀疏检出

- 文件：`internal/gitrepo/clone.go`、`internal/gitrepo/clone_test.go`；
- 红灯：扩展 `TestFetcher_CloneSingleBranchDepthOneWithoutTags` 要求 `NoCheckout=true`；新增
  `TestGoGitClient_CloneChecksOutRepositoryPolicy` 证明真实 go-git worktree 只落盘保留条目；
- 绿灯：CloneContext 返回后读取 HEAD tree，并以 `ResetSparsely` 检出策略结果；不改变接口、
  mirror、进度、清理和静态校验顺序；
- 验证：定向测试与 `internal/gitrepo -count=20`；提交实现。

## Task 3：真实 Git 组件矩阵与收口

- 文件：`internal/gitrepo/gitserver_test.go`、`internal/gitrepo/component_test.go`、权威文档；
- 红灯：HTTPS Depth 1 组件仓库中的 `frontend/` 与 `.debug` 仍落盘；
- 绿灯：扩展夹具写入任意受控文件，断言排除项缺失、保留项存在、Git status clean，既有
  shallow/remote/版本/对象边界不变；
- 验证：组件矩阵、标准门、`internal/gitrepo -count=20`、全仓 race detector、静态边界扫描；
  独立审查后删除本计划、归档设计并回写 T4.8 完成证据。
