# T12.9 Sentry 价值筛选与诊断加固实施计划

## 执行顺序

1. 先增加价值策略、凭据绕过、typed container、总大小和 `BeforeSend` 的失败测试，确认红灯。
2. 实现 telemetry 正向策略表和诊断 schema；CLI 停止传递原始 error；Sentry 二次重建。
3. 增加 uv runner、lock check 与 dependency sync 的稳定失败分类字段红灯，再补最小 details。
4. 跑定向测试、标准门、protocol 100 轮和完整 race；候选补丁通过后进行一轮独立只读绕过复审。

## 固定测试范围

- `TestFailureReportability_ExplicitPolicyForEveryFailureCode`
- `TestSanitizeDiagnostics_RedactsCredentialEncodingsAndUnknownTypes`
- `TestSanitizeDiagnostics_PreservesProductionTypedContainers`
- `TestSanitizeDiagnostics_BoundsCompletePayload`
- `TestSentry_BeforeSendRejectsUnreportableFailure`
- `TestSentry_InternalAndPanicDropFailureDiagnostics`
- `TestRunOperation_UnreportableFailureDoesNotReportSentry`
- `TestRunOperation_ExpectedFailureNeverUploadsRawError`
- `TestRunner_FailureDetailsExcludeCapturedOutput`
- `TestDependenciesService_FailureDetailsIdentifyUVPhase`

## 验证命令

全部命令使用 PowerShell 7，并逐条检查 `$LASTEXITCODE`：

```powershell
$env:GOCACHE = Join-Path $env:TEMP "auto-mas-runtime-t129"

& go test ./internal/telemetry ./internal/cli ./internal/uv -count=1
if ($LASTEXITCODE -ne 0) { throw "T12.9 focused tests failed" }

& go test ./internal/protocol -count=100
if ($LASTEXITCODE -ne 0) { throw "protocol repeated tests failed" }

& go test -race ./... -count=1
if ($LASTEXITCODE -ne 0) { throw "race tests failed" }
```

标准门另执行 gofmt 检查、`go vet ./...`、`go build -buildvcs=false ./...`、
`go test ./... -count=1` 与 `git diff --check`。任务未提交前保持 🚧，不得回写完成哈希或外发。
