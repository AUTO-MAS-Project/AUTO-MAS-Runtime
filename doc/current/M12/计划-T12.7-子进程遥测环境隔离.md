# T12.7 子进程遥测环境隔离实施计划

## 范围与边界

- 输入：`doc/current/M9/审查-M9-首版验收前全仓库对抗性Review.md` Important-1；该报告保持只读。
- 只修改 `internal/uv` 的环境构造及其真实子进程测试；不处理 Info-1 的一次性进程 Job 分配窗口。
- 不改变 NDJSON、错误四元组、stage、state、退出码、用户文案、`BuildSentryDSN` 或 Sentry adapter。

## Task 1：建立红灯测试

文件：`internal/uv/runner_test.go`、`internal/uv/managed_test.go`，必要时复用现有 fake uv helper。

- `TestRunner_ScrubsTelemetryEnvironment`：宿主环境与 `RunOptions.Environment` 注入四个遥测键和
  Windows 大小写变体，fake uv 读取完整环境；同时断言 `PATH`、普通变量和受控 `UV_*` 保留。
- `TestManagedRunner_ScrubsTelemetryEnvironment`：表驱动覆盖 managed/development 与
  enabled/disabled/offline，真实 `StartManaged` 子进程读取完整环境；断言四类键不存在且身份、
  supervision 和 `UV_OFFLINE` 语义正确。
- 定向命令必须验证非零红灯来自遥测键仍可见，并拒绝 `[no tests to run]`。

提交：`test: 增加子进程遥测环境隔离红灯测试 (T12.7)`。

## Task 2：最小实现并转绿

文件：`internal/uv/runner.go`。

- 在 `buildEnvironmentWithSupervision` 的共同入口维护本包私有的 Runtime-only 保留键集合；
- 对 `os.Environ()` 与调用选项均按 `strings.EqualFold` 清理，随后只按既有规则注入受控变量；
- 不导入 `internal/telemetry` 或 Sentry SDK，不扩大 allowlist，不改进程启动流程。

定向绿灯同时要求 exit 0、出现 `--- PASS:` 且不存在 `[no tests to run]`。

提交：`fix: 隔离子进程遥测环境变量 (T12.7)`。

## Task 3：验证、黑盒与复审

- 运行全仓 `gofmt`、`go vet`、`go build`、`go test`、`git diff --check`，协议包重复 100 轮及完整 race；
- 从当前源码构建新 EXE，用环境回显 fake uv/helper 验证 ordinary uv、managed backend、
  development backend 的 enabled/disabled/offline 矩阵、NDJSON、stderr、退出码、进程树与临时文件；
- 只读复审宿主/调用选项、大小写、必要环境、依赖方向、任务范围、真实子进程证据、前置门与协议契约；
- Critical/Important 清零后删除本计划，回写 T12.7 日期、实现提交短哈希和实际证据。

提交：`docs: 记录 T12.7 子进程遥测隔离完成情况`。
