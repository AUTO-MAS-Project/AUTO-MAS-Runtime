# T9.5 beta.4 development 黑盒回归修复实施计划

## Task 1：锁定 beta.4 回归

- 文件：`internal/backend/development_test.go`、`internal/cli/` 下 Windows 集成测试；
- 红灯：`TestBackendDevelopment_UVPreflightUsesExplicitRepo` 证明 development 预检未携带显式
  repo/.venv；黑盒基线证明无受管 repo 时 exit 50、`UV_EXEC_FAILED`、空 details；
- 绿灯：预检选项固定为显式 repo/.venv，managed 保持受管路径。

## Task 2：补稳定启动诊断

- 文件：`internal/uv/runner.go`、`internal/uv/runner_test.go`、`internal/uv/managed_test.go`；
- 红灯：`TestUVRunner_StartFailureIncludesStableDiagnostics`；
- 绿灯：创建失败 details 含白名单路径、`operation=start` 和可用的 Windows 数值错误码，
  不含环境或外部输出。

## Task 3：真实 Windows 集成与清理证明

- 文件：`internal/backend/e2e_windows_test.go`；
- 使用官方 uv 0.12.3、受管 Python 与真实 `.venv`；
- 覆盖 development `starting_backend -> running -> stopping_backend -> stopped`、status/shutdown、
  宿主 `UV_*` 和旧身份键清洗、源码 SHA-256 不变；
- 覆盖 Runtime 终止、shutdown、health 失败后 uv/python/孙进程和端口无残留。

## Task 4：验证与提交

```powershell
$env:GOCACHE = Join-Path $env:TEMP "auto-mas-runtime-verify"

$unformatted = & gofmt -l .
if ($LASTEXITCODE -ne 0) { throw "gofmt failed" }
if ($unformatted) { $unformatted; throw "gofmt found unformatted files" }
& go vet ./...; if ($LASTEXITCODE -ne 0) { throw "go vet failed" }
& go build -buildvcs=false ./...; if ($LASTEXITCODE -ne 0) { throw "build failed" }
& go test ./... -count=1; if ($LASTEXITCODE -ne 0) { throw "tests failed" }
& git diff --check; if ($LASTEXITCODE -ne 0) { throw "diff check failed" }
```

定向回归运行 100 轮；随后提升 GCC 目录到 `PATH` 首位并运行完整
`go test -race ./... -count=1`。每条原生命令都检查 `$LASTEXITCODE`。
