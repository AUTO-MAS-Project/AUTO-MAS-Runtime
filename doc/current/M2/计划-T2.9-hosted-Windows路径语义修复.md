# T2.9 hosted Windows 路径语义修复实施计划

## Task 1：登记失败与冻结边界

- 文件：`doc/任务拆分.md`、`doc/current/README.md`、本设计与计划。
- 完成条件：记录 Actions run `31344483252` 的失败范围；明确不改协议、CI workflow 与目录布局。
- 提交：`docs: 登记 hosted Windows 路径语义修复 (T2.9)`。

## Task 2：建立确定性红灯

- 文件：`internal/filesystem/inspect_windows_test.go`、`rename_windows_test.go`、
  `directory_windows_test.go`。
- 新增固定测试：
  - `TestInspectManagedDirectory_RejectsRawReparseBeforeCanonicalization`
  - `TestAtomicRename_RetriesPinSharingViolationAcrossEquivalentPathSpellings`
- 调整既有测试：`TestPrepareManagedDirectory_CreatesAndPinsExactTarget` 和
  `TestAtomicRename_CancellationAfterPinStopsBeforeMutation` 使用规范路径并保证句柄 cleanup。
- 红灯：新 Reparse 测试当前返回 success/type 错误；extended-path rename 测试当前不重试。
- 命令：

  ```powershell
  $env:GOCACHE = Join-Path $env:TEMP "auto-mas-runtime-t29-red"
  & go test ./internal/filesystem -run '^(TestInspectManagedDirectory_RejectsRawReparseBeforeCanonicalization|TestAtomicRename_RetriesPinSharingViolationAcrossEquivalentPathSpellings)$' -count=1 -v
  if ($LASTEXITCODE -eq 0) { throw "expected focused red tests to fail" }
  ```

## Task 3：最小实现与定向绿灯

- 文件：`internal/filesystem/path_windows.go`、`rename_windows.go` 及 Task 2 测试文件。
- 实现：raw 路径逐级 Reparse 检查；pin 内私有 retry marker；测试规范路径与 cleanup 修正。
- 定向验证：

  ```powershell
  $env:GOCACHE = Join-Path $env:TEMP "auto-mas-runtime-t29-focused"
  & go test ./internal/filesystem -run '^(TestPrepareManagedDirectory_CreatesAndPinsExactTarget|TestInspectManagedDirectory_RejectsDirectAndAncestorReparsePoints|TestInspectManagedDirectory_RejectsDanglingReparsePoints|TestInspectManagedDirectory_RejectsRawReparseBeforeCanonicalization|TestAtomicRename_RetriesPinSharingViolationBeforeRename|TestAtomicRename_RetriesPinSharingViolationAcrossEquivalentPathSpellings|TestAtomicRename_MapsPinSharingViolationAfterRetryExhaustion|TestAtomicRename_CancellationAfterPinStopsBeforeMutation)$' -count=100
  if ($LASTEXITCODE -ne 0) { throw "focused filesystem tests failed" }
  & go test ./internal/gitrepo -run '^TestComponent_GitReplacementMatrix/directory_handle_without_delete_share_maps_to_occupied$' -count=10 -v
  if ($LASTEXITCODE -ne 0) { throw "gitrepo occupied component test failed" }
  ```

- 提交：`fix: 收口 hosted Windows 路径失败语义 (T2.9)`。

## Task 4：全量验收与归档

- 执行 `gofmt -l .`、`go vet ./...`、`go build -buildvcs=false ./...`、
  `go test ./... -count=1`、`git diff --check`，每条检查 `$LASTEXITCODE`。
- 提升实际 GCC 目录到当前 `PATH` 首位后执行 `go test -race ./... -count=1`。
- 审查 stdout 所有权、变更文件范围和未提交 diff；不得 push。
- 将设计移入 `doc/archive/M2/`，删除本计划，回写 T2.9/M2 完成状态与实际提交哈希。
- 提交：`docs: 记录 T2.9 路径语义修复完成情况`。
