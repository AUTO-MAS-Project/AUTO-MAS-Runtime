# T13.13 同分支后端热更新检查计划

## Task 1：文档与协议结果

- 文件：`doc/架构设计.md`、`doc/任务拆分.md`
- 红灯：补充同分支更新语义和 `workspace check --remote` 结果字段。
- 绿灯：文档明确只读检查、Commit no-op 判定和重启应用语义。

## Task 2：Runtime 远端检查与同步判定

- 文件：`internal/gitrepo/repository.go`、`internal/gitrepo/service.go`、`internal/cli/workspace.go` 及测试。
- 红灯：同版本不同 Commit 不再 no-op；远端检查能返回分支 Commit。
- 绿灯：镜像轮换、错误映射、无目录副作用测试通过。

## Task 3：Runtime 后台暂存与启动复用

- 文件：`internal/gitrepo/service.go`、`internal/gitrepo/recovery.go`、`internal/state/transaction.go`、`internal/cli/workspace.go`。
- 红灯：后端运行期间无法准备仓库，启动时无法识别已校验暂存目录。
- 绿灯：`workspace stage` 保留 `update.json`，下次 `workspace sync/bootstrap` 复用暂存目录并完成原子 swap。

## Task 4：Electron 接入

- 文件：Runtime client、`backendService`、IPC/preload、版本服务和标题栏。
- 红灯：managed 模式没有远端检查结果或无法显示重启提示。
- 绿灯：周期检查后立即后台 stage，成功后提示下次启动生效；off/development 不检查。

## 验证

Runtime 执行 gofmt、go vet、go build、go test 和 diff check。AUTO-MAS frontend 执行仓库既定 lint、typecheck、test、build 最小门。

## 本地验收记录（2026-09-05）

- 已实现：只读远端检查、后台 stage、目标和基础 Commit 绑定、同版本启动切换、跨版本废弃、
  同 Commit no-op、mutation/backend Mutex 协调、中断恢复与 bootstrap --if-needed。
- 已接入：Electron 检查去重、就绪缓存、退出取消、managed 启动、标题栏就绪提示、
  本地打包指定 Runtime 和 CI 协议门禁。
- 定向覆盖：`TestService_StageAndActivateOffline`、`TestService_StageConflictDoesNotWriteTransactions`、
  `TestService_StagedUpdateDoesNotBlockVersionUpgrade`、`TestService_SyncSameFetchedCommitKeepsEnvironment`、
  `TestService_StageInterruptedCloneRecovers`、`TestService_StagedCommitMismatchFailsClosed`、
  `TestRecovery_StagedSameVersionSwapWindows`、`TestService_CancelAfterRecoveryPreservesStagedOwnership`、
  `TestRemoteResolve_MirrorFallbackAndFailures`、`TestWorkspaceStageContract`、`TestWorkspaceRemoteContract`、
  `TestWorkspaceStage_StdinCancel`、`TestBootstrap_IfNeededSkipsOnlyUnchangedReadyEnvironment`。
- Runtime：gofmt、go vet ./...、go build -buildvcs=false ./...、go test ./... -count=1、
  git diff --check 通过。GOCACHE 使用临时目录 auto-mas-runtime-final2。
- 并发：protocol 全包 100 轮、staging mutex 定向 100 轮通过；完整 go test -race ./... -count=1
  在本机提升 GCC 路径后通过（沙箱内 cgo 环境失败不计为通过证据）。
- 前端：41 个测试文件、392 项测试通过；Vue 与 Electron 类型检查、lint、生产 Vite 构建通过。
  构建输出为临时目录 auto-mas-hotupdate-web；测试和构建会话清空 Sentry 上传凭据，不外发。
- 本地 EXE：bin/auto-mas-runtime.exe 已生成，本地打包脚本 -VerifyRuntimeOnly 模式退出码 0；
  stage 与 bootstrap --if-needed 的真实 NDJSON 探测通过。
- 未执行：完整安装包重启桌面 E2E、远端 Release 与 CI 运行；未提交或推送。
  发布需要先发布包含本任务的新 Runtime，再更新 AUTO-MAS 的 RUNTIME_VERSION。
  T13.13 保留进行中，待提交与发布验收后按 DoD 收口。
