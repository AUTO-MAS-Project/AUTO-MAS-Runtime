# T5.11 锁文件 sources 语义回归修复计划

## Task 1：冻结真实失败反例

- 文件：`internal/uv/dependencies_test.go`
- 红灯：将锁检查策略测试改为要求在线沿用锁 sources；当前 T5.10 实现会额外传
  `--default-index`，默认在线和显式首选子用例失败，离线子用例通过。
- 验证：定向测试退出非零、包含两个预期失败和一个 PASS，不含 `[no tests to run]`。
- 提交：只提交测试文件。

## Task 2：最小修复

- 文件：`internal/uv/dependencies.go`
- 绿灯：lock check 只按 `MirrorPolicy.Offline()` 决定是否注入 `UV_OFFLINE=1`；在线不再
  构造 package-index plan 或覆盖锁 sources。sync 网络执行路径保持不变。
- 验证：策略测试、锁文件契约、sync 首选和轮换测试通过。
- 提交：只提交实现文件。

## Task 3：验收与归档

- 执行依赖测试 20 轮、协议 100 轮、全仓标准门和完整 race。
- 构建带版本信息的新 EXE；把本地 AUTO-MAS 测试分支已修正的 `uv.lock` 注入黑盒受管 repo，
  运行 `dependencies sync`，校验成功状态、锁哈希和新 EXE 身份。
- 独立检查远端分支仍过期的边界，不 push；归档设计、删除计划并回写任务状态。
