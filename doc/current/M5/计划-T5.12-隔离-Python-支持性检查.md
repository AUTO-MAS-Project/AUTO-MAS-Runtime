# T5.12 隔离 Python 支持性检查实施计划

## Task 1：冻结失败复现

- 文件：`internal/uv/python_test.go`
- 红灯：新增测试证明支持性查询未限定精确版本和可下载目录，并证明 uv 执行失败与无效 JSON
  被误报为 `PYTHON_VERSION_UNSUPPORTED`。
- 验证：定向运行新增测试，确认退出码非零、出现 `--- FAIL:` 且失败原因命中上述行为。

## Task 2：最小修复

- 文件：`internal/uv/python.go`
- 绿灯：查询参数加入精确版本与 `--only-downloads`；保留执行器结构化错误；非零退出和无效
  JSON 映射既有 `UV_EXEC_FAILED`；有效目录缺少目标版本仍映射既有不支持错误。
- 验证：新增测试与 `go test ./internal/uv -count=1` 全绿。

## Task 3：真实 uv 与全仓验收

- 文件：`doc/任务拆分.md`、`doc/archive/M5/设计-T5.12-隔离-Python-支持性检查.md`
- 验证：真实 uv 0.12.3 在污染 `PATH` 的当前进程中执行精确可下载目录查询；运行标准验证门、
  stdout 所有权扫描与完整 race detector。
- 收口：删除本计划，将设计移入归档，回写任务完成证据与变更记录，再按任务范围提交。
