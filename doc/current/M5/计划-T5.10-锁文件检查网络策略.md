# T5.10 锁文件检查网络策略回归修复计划

## Task 1：冻结在线与离线红灯

- 文件：`internal/uv/dependencies_test.go`
- 红灯：新增 `TestDependencies_LockfileCheckFollowsMirrorPolicy`，证明当前默认在线和显式首选
  仍错误注入 `UV_OFFLINE=1`，且未携带 package-index 参数；离线子用例保持正确。
- 验证：定向命令必须退出非零，输出包含预期断言失败且不含 `[no tests to run]`。
- 提交：只提交测试文件。

## Task 2：最小实现策略选择

- 文件：`internal/uv/network.go`、`internal/uv/dependencies.go`。
- 绿灯：复用 `networkExecutor` 的冻结计划，在线检查选择首个 package-index 源并追加
  `--default-index`；离线检查只注入 `UV_OFFLINE=1`。保持既有错误映射和 sync 轮换不变。
- 验证：定向测试通过并重复运行；既有 `TestDependencies_LockfileContract` 通过。
- 提交：只提交实现文件。

## Task 3：全量验收与回写

- 运行 `gofmt`、`go vet ./...`、`go build -buildvcs=false ./...`、`go test ./... -count=1`、
  `git diff --check`、M5 定向重复、协议 100 轮及完整 race detector。
- 审查最终 diff，确认无 stdout、Shell、系统 Python/pip 或协议契约变化。
- 删除本计划，将设计归档到 `doc/archive/M5/`，回写 `doc/任务拆分.md` 与归档索引。
- 提交：只提交任务状态和归档文档。
