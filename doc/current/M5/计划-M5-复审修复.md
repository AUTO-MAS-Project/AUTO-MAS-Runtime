# M5 复审修复实施计划

日期：2026-08-09

每个 Task 严格先写红灯并确认失败原因，再写最小实现；每个 Task 独立暂存、验证和提交。

## Task 1：文档纠偏

- 文件：`AGENTS.md`、`doc/current/README.md`、`doc/current/M5/*`、`doc/任务拆分.md`。
- 验证：文档链接、`git diff --check`、暂存清单。
- 提交：`docs: 重开 M5 复审修复任务 (T5.1-T5.8)`。

## Task 2：真实 uv 资产

- 文件：`internal/uv/bootstrap.go`、`runner.go` 及对应测试、integration 测试。
- 红灯：`TestZipExtractor_OfficialLayout`、`TestCheckVersion_OfficialOutput`、
  `TestIntegration_OfficialUVArtifact`。
- 绿灯：已知三成员 ZIP 只发布 `uv.exe`；真实版本输出通过，伪装/未知成员仍失败。
- 验证：`go test ./internal/uv -run 'Test(ZipExtractor|CheckVersion|Bootstrap)' -count=10`；
  显式资产下 `go test -tags=integration ./internal/uv -run TestIntegration_OfficialUVArtifact -v`。
- 提交：`fix: 兼容官方 uv 0.12.3 资产 (T5.1)`。

## Task 3：受控环境与网络策略

- 文件：`internal/uv/runner.go`、网络策略实现、Python/依赖/production/environment 服务及测试。
- 红灯：`TestRunner_ScrubsUnmanagedUVEnvironment`、`TestPython_MirrorPolicy`、
  `TestDependencies_PackageIndexPolicy`、`TestNetworkPolicy_Offline`。
- 绿灯：继承的 `UV_*` 不可生效；四类 CLI policy 贯通 Python/包索引；TLS/sources 不被关闭；
  mirror 轮换目标不变。
- 验证：`go test ./internal/uv ./internal/cli -run 'Test(Runner|Python|Dependencies|Network)' -count=10`。
- 提交：`fix: 贯通 uv 受控网络策略 (T5.2-T5.4)`。

## Task 4：恢复、repair 与状态事实

- 文件：`internal/uv/environment.go`、`python.go`、production 适配、
  `internal/cli/environment_support.go`、bootstrap/environment/dependencies/repair 及测试。
- 红灯：`TestRecoverM5Transaction_InvalidatesReadyState`、`TestRepair_ReinstallsPython`、
  `TestRepair_StagesFollowExecution`、`TestM5Failure_PersistsKnownToolVersions`。
- 绿灯：危险阶段 stale transaction 先 broken 后清理；repair 强制重装；事件/事务与真实阶段同步；
  Python/依赖失败保留精确工具事实。
- 验证：相关定向测试 `-count=10`，事务并发/恢复测试 `-count=100`。
- 提交：`fix: 收紧 M5 修复与崩溃恢复 (T5.5-T5.7)`。

## Task 5：依赖检查与版本规范

- 文件：`internal/uv/dependencies.go`、`python.go` 及测试。
- 红灯：`TestDependencies_CheckDetectsUnsynchronizedEnvironment`、
  `TestPython_CompatibleReleaseSpecifiers`、`TestPython_ArrayTableIsNotProject`。
- 绿灯：check 同时证明 lock 与 venv；兼容版本与 TOML 表归属符合冻结语义。
- 验证：`go test ./internal/uv -run 'Test(Dependencies|Python)' -count=10`。
- 提交：`fix: 校验依赖环境与 Python 约束 (T5.3-T5.4)`。

## Task 6：CLI 契约与组件矩阵

- 文件：M5 CLI 命令/帮助、`contract_*_test.go`、M5 组件夹具与测试。
- 红灯：M5 contract 注册、capability/help/stage、中断重跑与保护目录用例。
- 绿灯：全部命令 hello/result/error/state 契约通过；fresh root 和 cutpoint 重跑达到 ready；
  用户数据/插件/源码/锁文件快照不变。
- 验证：M5 contract/component 定向测试 `-count=10`，协议包 `-count=100`。
- 提交：`test: 补齐 M5 契约与组件矩阵 (T5.5-T5.8)`。

## Task 7：最终门禁与进度回写

- 标准门：`gofmt -l .`、`go vet ./...`、`go build -buildvcs=false ./...`、
  `go test ./... -count=1`、`git diff --check`。
- 追加门：M5 包 `-count=10`、协议 `-count=100`、真实 uv integration、沙箱外完整 race、
  stdout/uv/Shell/直接 Python/pip 静态扫描。
- Critical/Important 清零后将设计归档、删除本计划并逐项回写 T5.1~T5.8。
- 提交：`docs: 记录 M5 复审修复完成情况`。
