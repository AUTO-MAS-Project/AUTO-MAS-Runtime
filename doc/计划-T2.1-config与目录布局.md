# T2.1 config 与目录布局实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用
> `superpowers:subagent-driven-development`（推荐）或
> `superpowers:executing-plans` 逐任务实现此计划。步骤使用复选框（`- [ ]`）
> 语法跟踪进度；每个任务提交前先做规格审查，再做代码质量审查。

**目标：** 实现不可变的 `config.Layout`，以显式 base 解析 app root，并集中提供
全部受管目录、动态单段路径和根级保护名单。

**架构：** `layout.go` 只负责 app root 的词法解析与身份键；`paths.go` 保存并暴露
预计算的固定布局；`segment.go` 统一校验动态 Windows 文件名 segment；
`protection.go` 返回防御性保护名单。整个包不访问文件系统、不读取当前目录，也不承担
物理路径或 Reparse Point 校验。

**技术栈：** Go 1.26，标准库 `errors`、`path/filepath`、`strings`、`time`、
`unicode/utf16`、`testing`；Windows + PowerShell 7；不新增第三方依赖。

**设计依据：** [T2.1 config 与目录布局设计](./设计-T2.1-config与目录布局.md)，
代码基线 `fb06f57`。

---

## 文件结构

- 保留 `internal/config/doc.go`
  - 现有中文包注释，无需修改。
- 创建 `internal/config/errors.go`
  - 定义设计冻结的七个 sentinel error。
- 创建 `internal/config/layout.go`
  - 实现 `Layout`、`NewLayout`、`AppRoot` 与 `IdentityKey`。
- 创建 `internal/config/layout_test.go`
  - 覆盖显式 base、非法根路径、身份键、不展开语法和零文件系统副作用。
- 创建 `internal/config/paths.go`
  - 预计算并暴露 repo、state、runtime、uv、Python、venv、三类缓存、日志及根级数据目录。
- 创建 `internal/config/paths_test.go`
  - 逐项比对固定目录，检查绝对路径、唯一 state 文件与受管根边界。
- 创建 `internal/config/segment.go`
  - 校验动态 Windows 单段名称，并实现 repo 事务目录、uv 版本目录、下载文件、
    uv staging 目录和日志文件路径。
- 创建 `internal/config/segment_test.go`
  - 覆盖合法值原样保留、全部 ASCII 控制字符、设备名、UTF-16 长度、零时间
    和本地日期。
- 创建 `internal/config/segment_internal_test.go`
  - 白盒覆盖追加 `.part` 后恰好 255 与超过 255 UTF-16 code unit 的派生边界。
- 创建 `internal/config/protection.go`
  - 按冻结顺序返回七个根级保护目录的防御性副本。
- 创建 `internal/config/protection_test.go`
  - 覆盖保护名单顺序、防御性副本与全部 getter 的并发只读。
- 修改 `doc/任务拆分.md`
  - 全部验收通过后勾选 T2.1 并记录最新代码提交短哈希。

## 公共 PowerShell 7 前置

每个任务开始时在
`D:\Github\AUTO-MAS-Runtime\.worktrees\codex-runtime-implementation`
执行：

```powershell
$ErrorActionPreference = "Stop"
$go = (Get-Command go -ErrorAction Stop).Source
$gofmt = (Get-Command gofmt -ErrorAction Stop).Source
$env:GOCACHE = Join-Path $env:TEMP "auto-mas-runtime-t21"

function Invoke-Checked {
    param(
        [scriptblock]$Command,
        [string]$Name
    )

    $output = @(& $Command 2>&1)
    $exitCode = $LASTEXITCODE
    $output
    if ($exitCode -ne 0) {
        throw "$Name failed with exit code $exitCode"
    }
}

function Invoke-ExpectedFailure {
    param(
        [string]$Pattern,
        [string]$ExpectedFailure
    )

    $output = @(& $go test ./internal/config -run $Pattern -count=1 -v 2>&1)
    $exitCode = $LASTEXITCODE
    $output
    $text = $output -join [Environment]::NewLine
    if ($exitCode -eq 0) {
        throw "red test unexpectedly passed"
    }
    if ($text -match "\[no tests to run\]" -or $text -notmatch "(build failed|--- FAIL:)") {
        throw "red test did not execute and fail"
    }
    if ($text -notmatch $ExpectedFailure) {
        throw "red test failed for an unexpected reason"
    }
}

function Invoke-PassingTest {
    param([string]$Pattern)

    $output = @(& $go test ./internal/config -run $Pattern -count=1 -v 2>&1)
    $exitCode = $LASTEXITCODE
    $output
    $text = $output -join [Environment]::NewLine
    if ($exitCode -ne 0) {
        throw "targeted test failed with exit code $exitCode"
    }
    if ($text -match "\[no tests to run\]" -or $text -notmatch "--- PASS:") {
        throw "targeted test did not produce a passing result"
    }
}
```

所有原生命令必须立即读取并检查 `$LASTEXITCODE`。定向测试必须同时证明退出码、
`[no tests to run]` 和 `--- PASS:` 三项。

## 任务 1：app root 词法解析与身份键

**文件：**

- 创建：`internal/config/errors.go`
- 创建：`internal/config/layout.go`
- 创建：`internal/config/layout_test.go`

- [ ] **步骤 1：编写固定名称的失败测试**

`layout_test.go` 使用外部包 `config_test`，固定以下测试函数名：

```go
func TestNewLayout_ResolvesExplicitBase(t *testing.T)
func TestNewLayout_RejectsInvalidInputs(t *testing.T)
func TestNewLayout_DoesNotExpandShellSyntax(t *testing.T)
func TestNewLayout_DoesNotReadCurrentDirectory(t *testing.T)
func TestLayout_IdentityKeyIsCaseInsensitive(t *testing.T)
func TestNewLayout_DoesNotTouchFilesystem(t *testing.T)
```

核心测试形态：

```go
func TestNewLayout_ResolvesExplicitBase(t *testing.T) {
    base := t.TempDir()
    tests := []struct {
        name    string
        appRoot string
        want    string
    }{
        {name: "relative", appRoot: "AUTO-MAS", want: filepath.Join(base, "AUTO-MAS")},
        {name: "dot", appRoot: ".", want: filepath.Clean(base)},
        {
            name:    "parent and trailing separator",
            appRoot: filepath.Join("nested", "..", "AUTO-MAS") + string(filepath.Separator),
            want:    filepath.Join(base, "AUTO-MAS"),
        },
        {
            name:    "absolute",
            appRoot: filepath.Join(base, "AbsoluteRoot"),
            want:    filepath.Join(base, "AbsoluteRoot"),
        },
    }

    for _, test := range tests {
        t.Run(test.name, func(t *testing.T) {
            layout, err := config.NewLayout(test.appRoot, base)
            if err != nil {
                t.Fatalf("NewLayout(%q, %q) error = %v", test.appRoot, base, err)
            }
            if got := layout.AppRoot(); got != filepath.Clean(test.want) {
                t.Fatalf("AppRoot() = %q, want %q", got, filepath.Clean(test.want))
            }
        })
    }
}
```

`TestNewLayout_RejectsInvalidInputs` 表驱动覆盖并用 `errors.Is` 断言：

| 输入 | 预期 sentinel |
| --- | --- |
| 空 app root / 空 base | `config.ErrEmptyPath` |
| app root / base 含 NUL | `config.ErrPathContainsNUL` |
| 相对 base | `config.ErrBaseNotAbsolute` |
| `C:relative` app root | `config.ErrInvalidPath` |
| 当前盘符根 | `config.ErrAppRootIsRoot` |
| `\\server\share\` | `config.ErrAppRootIsRoot` |

`TestNewLayout_DoesNotExpandShellSyntax` 逐项传
`~\AUTO-MAS`、`%LOCALAPPDATA%\AUTO-MAS`、`$HOME\AUTO-MAS`，断言它们作为普通文本
位于显式 base 下。

`TestNewLayout_DoesNotReadCurrentDirectory` 保存并恢复 cwd，把进程 cwd 切到另一个
临时目录后仍断言相对 app root 解析到传入 base。该测试不得调用 `t.Parallel()`。

`TestNewLayout_DoesNotTouchFilesystem` 传一个尚不存在的 app root，构造前后均用
`os.Stat` 断言 `errors.Is(err, fs.ErrNotExist)`。

- [ ] **步骤 2：运行红灯并确认缺失 API**

```powershell
Invoke-ExpectedFailure `
    "Test(NewLayout|Layout_IdentityKey)" `
    "undefined: config\.(NewLayout|ErrEmptyPath)"
```

预期：测试编译失败，明确报告 `config.NewLayout` 或 sentinel 尚未定义；不得接受路径
断言写错、测试未运行等其他失败原因。

- [ ] **步骤 3：实现 sentinel error**

`errors.go` 的最小实现固定为：

```go
package config

import "errors"

var (
    // ErrEmptyPath 表示必需路径为空。
    ErrEmptyPath       = errors.New("path is empty")
    // ErrPathContainsNUL 表示路径包含 NUL。
    ErrPathContainsNUL = errors.New("path contains nul")
    // ErrBaseNotAbsolute 表示显式 base 不是绝对路径。
    ErrBaseNotAbsolute = errors.New("base path is not absolute")
    // ErrInvalidPath 表示路径形式无法安全解析。
    ErrInvalidPath     = errors.New("path is invalid")
    // ErrAppRootIsRoot 表示 app root 指向卷根。
    ErrAppRootIsRoot   = errors.New("app root is a volume root")
    // ErrInvalidSegment 表示动态文件名 segment 非法。
    ErrInvalidSegment  = errors.New("path segment is invalid")
    // ErrInvalidLogDate 表示日志日期为零值。
    ErrInvalidLogDate  = errors.New("log date is invalid")
)
```

- [ ] **步骤 4：实现 Layout 与纯词法解析**

`layout.go` 保持以下形态：

```go
package config

import (
    "fmt"
    "path/filepath"
    "strings"
)

// Layout 保存一个 app root 的不可变受管目录布局。
type Layout struct {
    appRoot     string
    identityKey string
}

// NewLayout 以显式绝对 base 解析 app root，不访问文件系统。
func NewLayout(appRoot, base string) (*Layout, error) {
    if appRoot == "" || base == "" {
        return nil, ErrEmptyPath
    }
    if strings.ContainsRune(appRoot, '\x00') || strings.ContainsRune(base, '\x00') {
        return nil, ErrPathContainsNUL
    }
    if !filepath.IsAbs(base) {
        return nil, ErrBaseNotAbsolute
    }
    if filepath.VolumeName(appRoot) != "" && !filepath.IsAbs(appRoot) {
        return nil, ErrInvalidPath
    }

    resolved := appRoot
    if !filepath.IsAbs(resolved) {
        resolved = filepath.Join(filepath.Clean(base), resolved)
    }
    resolved = filepath.Clean(resolved)
    if !filepath.IsAbs(resolved) {
        return nil, fmt.Errorf("resolve app root: %w", ErrInvalidPath)
    }
    if isVolumeRoot(resolved) {
        return nil, ErrAppRootIsRoot
    }

    return &Layout{
        appRoot:     resolved,
        identityKey: identityKey(resolved),
    }, nil
}

// AppRoot 返回规范化的显示与文件 I/O 路径。
func (l *Layout) AppRoot() string {
    return l.appRoot
}

// IdentityKey 返回大小写不敏感的词法身份键。
func (l *Layout) IdentityKey() string {
    return l.identityKey
}

func identityKey(path string) string {
    normalized := filepath.Clean(path)
    normalized = strings.ReplaceAll(normalized, "/", `\`)
    return strings.ToLower(normalized)
}

func isVolumeRoot(path string) bool {
    volume := filepath.VolumeName(path)
    if volume == "" {
        return false
    }
    root := filepath.Clean(volume + string(filepath.Separator))
    return strings.EqualFold(filepath.Clean(path), root)
}
```

所有新增注释按仓库规范使用中文，error 字符串保持英文小写。

- [ ] **步骤 5：格式化并验证绿灯**

```powershell
Invoke-Checked {
    & $gofmt -w `
        "internal/config/errors.go" `
        "internal/config/layout.go" `
        "internal/config/layout_test.go"
} "T2.1 Task 1 gofmt"
Invoke-PassingTest "Test(NewLayout|Layout_IdentityKey)"
Invoke-Checked { & $go test ./... -count=1 } "T2.1 Task 1 go test ./..."
Invoke-Checked { git diff --check } "T2.1 Task 1 git diff --check"
```

预期：六个固定名称测试出现 `--- PASS:`，全仓测试退出码 0。

- [ ] **步骤 6：只暂存任务 1 文件并提交**

```powershell
& git add -- `
    "internal/config/errors.go" `
    "internal/config/layout.go" `
    "internal/config/layout_test.go"
if ($LASTEXITCODE -ne 0) { throw "T2.1 Task 1 staging failed" }

$staged = @(& git diff --cached --name-only)
if ($LASTEXITCODE -ne 0) { throw "T2.1 Task 1 staged list failed" }
$expected = @(
    "internal/config/errors.go",
    "internal/config/layout.go",
    "internal/config/layout_test.go"
)
$delta = @(Compare-Object ($staged | Sort-Object) ($expected | Sort-Object))
if ($delta.Count -ne 0) { $delta; throw "T2.1 Task 1 staged unexpected files" }

& git commit -m "feat: 解析应用根目录与路径身份 (T2.1)"
if ($LASTEXITCODE -ne 0) { throw "T2.1 Task 1 commit failed" }
```

## 任务 2：固定受管目录布局

**文件：**

- 修改：`internal/config/layout.go`
- 创建：`internal/config/paths.go`
- 创建：`internal/config/paths_test.go`

- [ ] **步骤 1：编写固定路径失败测试**

固定测试函数名：

```go
func TestLayout_FixedPathsMatchSpecification(t *testing.T)
func TestLayout_FixedPathsAreAbsolute(t *testing.T)
func TestLayout_StateFilesAreDistinct(t *testing.T)
func TestLayout_AllFixedPathsStayWithinAppRoot(t *testing.T)
```

`TestLayout_FixedPathsMatchSpecification` 必须逐项列出全部 getter：

```go
func TestLayout_FixedPathsMatchSpecification(t *testing.T) {
    root := filepath.Join(t.TempDir(), "AUTO-MAS")
    layout, err := config.NewLayout(root, filepath.Dir(root))
    if err != nil {
        t.Fatal(err)
    }

    tests := []struct {
        name string
        got  string
        want string
    }{
        {"RepoDir", layout.RepoDir(), filepath.Join(root, "repo")},
        {"StateDir", layout.StateDir(), filepath.Join(root, "runtime-state")},
        {"BackendStateFile", layout.BackendStateFile(), filepath.Join(root, "runtime-state", "backend.json")},
        {"MutationStateFile", layout.MutationStateFile(), filepath.Join(root, "runtime-state", "mutation.json")},
        {"UpdateStateFile", layout.UpdateStateFile(), filepath.Join(root, "runtime-state", "update.json")},
        {"EnvironmentStateFile", layout.EnvironmentStateFile(), filepath.Join(root, "runtime-state", "environment.json")},
        {"RuntimeDir", layout.RuntimeDir(), filepath.Join(root, "runtime")},
        {"UVToolsDir", layout.UVToolsDir(), filepath.Join(root, "runtime", "tools", "uv")},
        {"PythonDir", layout.PythonDir(), filepath.Join(root, "runtime", "environment", "python")},
        {"VenvDir", layout.VenvDir(), filepath.Join(root, "runtime", "environment", "venv")},
        {"RuntimeCacheDir", layout.RuntimeCacheDir(), filepath.Join(root, "runtime", "cache")},
        {"UVCacheDir", layout.UVCacheDir(), filepath.Join(root, "runtime", "cache", "uv")},
        {"DownloadCacheDir", layout.DownloadCacheDir(), filepath.Join(root, "runtime", "cache", "downloads")},
        {"BuildCacheDir", layout.BuildCacheDir(), filepath.Join(root, "runtime", "cache", "build")},
        {"LogsDir", layout.LogsDir(), filepath.Join(root, "logs")},
        {"RuntimeLogDir", layout.RuntimeLogDir(), filepath.Join(root, "logs", "runtime")},
        {"ConfigDir", layout.ConfigDir(), filepath.Join(root, "config")},
        {"DataDir", layout.DataDir(), filepath.Join(root, "data")},
        {"HistoryDir", layout.HistoryDir(), filepath.Join(root, "history")},
        {"ScriptDir", layout.ScriptDir(), filepath.Join(root, "script")},
        {"DebugDir", layout.DebugDir(), filepath.Join(root, "debug")},
        {"PluginsDir", layout.PluginsDir(), filepath.Join(root, "plugins")},
    }
    for _, test := range tests {
        t.Run(test.name, func(t *testing.T) {
            if test.got != test.want {
                t.Fatalf("got %q, want %q", test.got, test.want)
            }
        })
    }
}
```

绝对路径测试对同一 getter 集合逐项调用 `filepath.IsAbs`。受管根测试用
`filepath.Rel(layout.AppRoot(), path)`，拒绝结果等于 `..` 或以
`..\` 开头。state 文件测试断言四个结果互不相同且父目录均为 `StateDir()`。

- [ ] **步骤 2：运行红灯并确认 getter 缺失**

```powershell
Invoke-ExpectedFailure `
    "TestLayout_(FixedPaths|StateFiles|AllFixedPaths)" `
    "layout\.(RepoDir|StateDir) undefined"
```

预期：编译失败，报告 `*config.Layout` 尚无固定路径 getter。

- [ ] **步骤 3：预计算全部固定路径**

在 `Layout` 增加 `paths layoutPaths` 字段，并在 NewLayout 返回值中增加：

```go
paths: newLayoutPaths(resolved),
```

`paths.go` 定义：

```go
type layoutPaths struct {
    repoDir              string
    stateDir             string
    backendStateFile     string
    mutationStateFile    string
    updateStateFile      string
    environmentStateFile string
    runtimeDir           string
    uvToolsDir           string
    pythonDir            string
    venvDir              string
    runtimeCacheDir      string
    uvCacheDir           string
    downloadCacheDir     string
    buildCacheDir        string
    logsDir              string
    runtimeLogDir        string
    configDir            string
    dataDir              string
    historyDir           string
    scriptDir            string
    debugDir             string
    pluginsDir           string
}

func newLayoutPaths(root string) layoutPaths {
    stateDir := filepath.Join(root, "runtime-state")
    runtimeDir := filepath.Join(root, "runtime")
    environmentDir := filepath.Join(runtimeDir, "environment")
    cacheDir := filepath.Join(runtimeDir, "cache")
    logsDir := filepath.Join(root, "logs")

    return layoutPaths{
        repoDir:              filepath.Join(root, "repo"),
        stateDir:             stateDir,
        backendStateFile:     filepath.Join(stateDir, "backend.json"),
        mutationStateFile:    filepath.Join(stateDir, "mutation.json"),
        updateStateFile:      filepath.Join(stateDir, "update.json"),
        environmentStateFile: filepath.Join(stateDir, "environment.json"),
        runtimeDir:           runtimeDir,
        uvToolsDir:           filepath.Join(runtimeDir, "tools", "uv"),
        pythonDir:            filepath.Join(environmentDir, "python"),
        venvDir:              filepath.Join(environmentDir, "venv"),
        runtimeCacheDir:      cacheDir,
        uvCacheDir:           filepath.Join(cacheDir, "uv"),
        downloadCacheDir:     filepath.Join(cacheDir, "downloads"),
        buildCacheDir:        filepath.Join(cacheDir, "build"),
        logsDir:              logsDir,
        runtimeLogDir:        filepath.Join(logsDir, "runtime"),
        configDir:            filepath.Join(root, "config"),
        dataDir:              filepath.Join(root, "data"),
        historyDir:           filepath.Join(root, "history"),
        scriptDir:            filepath.Join(root, "script"),
        debugDir:             filepath.Join(root, "debug"),
        pluginsDir:           filepath.Join(root, "plugins"),
    }
}
```

- [ ] **步骤 4：实现全部固定 getter**

`paths.go` 为 22 个固定结果提供直接 getter。形态固定为：

```go
func (l *Layout) RepoDir() string              { return l.paths.repoDir }
func (l *Layout) StateDir() string             { return l.paths.stateDir }
func (l *Layout) BackendStateFile() string      { return l.paths.backendStateFile }
func (l *Layout) MutationStateFile() string     { return l.paths.mutationStateFile }
func (l *Layout) UpdateStateFile() string       { return l.paths.updateStateFile }
func (l *Layout) EnvironmentStateFile() string  { return l.paths.environmentStateFile }
func (l *Layout) RuntimeDir() string             { return l.paths.runtimeDir }
func (l *Layout) UVToolsDir() string             { return l.paths.uvToolsDir }
func (l *Layout) PythonDir() string              { return l.paths.pythonDir }
func (l *Layout) VenvDir() string                { return l.paths.venvDir }
func (l *Layout) RuntimeCacheDir() string        { return l.paths.runtimeCacheDir }
func (l *Layout) UVCacheDir() string             { return l.paths.uvCacheDir }
func (l *Layout) DownloadCacheDir() string       { return l.paths.downloadCacheDir }
func (l *Layout) BuildCacheDir() string          { return l.paths.buildCacheDir }
func (l *Layout) LogsDir() string                { return l.paths.logsDir }
func (l *Layout) RuntimeLogDir() string           { return l.paths.runtimeLogDir }
func (l *Layout) ConfigDir() string              { return l.paths.configDir }
func (l *Layout) DataDir() string                { return l.paths.dataDir }
func (l *Layout) HistoryDir() string             { return l.paths.historyDir }
func (l *Layout) ScriptDir() string              { return l.paths.scriptDir }
func (l *Layout) DebugDir() string               { return l.paths.debugDir }
func (l *Layout) PluginsDir() string             { return l.paths.pluginsDir }
```

删除 `layout.go` 中任务 1 的空 `layoutPaths/newLayoutPaths` 声明。不得在 getter 中读取
环境变量、cwd 或文件系统。

- [ ] **步骤 5：验证绿灯**

```powershell
Invoke-Checked {
    & $gofmt -w `
        "internal/config/layout.go" `
        "internal/config/paths.go" `
        "internal/config/paths_test.go"
} "T2.1 Task 2 gofmt"
Invoke-PassingTest "TestLayout_(FixedPaths|StateFiles|AllFixedPaths)"
Invoke-Checked { & $go test ./... -count=1 } "T2.1 Task 2 go test ./..."
Invoke-Checked { git diff --check } "T2.1 Task 2 git diff --check"
```

- [ ] **步骤 6：提交任务 2**

```powershell
& git add -- `
    "internal/config/layout.go" `
    "internal/config/paths.go" `
    "internal/config/paths_test.go"
if ($LASTEXITCODE -ne 0) { throw "T2.1 Task 2 staging failed" }
$staged = @(& git diff --cached --name-only)
if ($LASTEXITCODE -ne 0) { throw "T2.1 Task 2 staged list failed" }
$expected = @(
    "internal/config/layout.go",
    "internal/config/paths.go",
    "internal/config/paths_test.go"
)
$delta = @(Compare-Object ($staged | Sort-Object) ($expected | Sort-Object))
if ($delta.Count -ne 0) { $delta; throw "T2.1 Task 2 staged unexpected files" }
& git commit -m "feat: 集中定义受管目录布局 (T2.1)"
if ($LASTEXITCODE -ne 0) { throw "T2.1 Task 2 commit failed" }
```

## 任务 3：动态 segment、下载文件与日志文件路径

**文件：**

- 创建：`internal/config/segment.go`
- 创建：`internal/config/segment_test.go`
- 创建：`internal/config/segment_internal_test.go`

- [ ] **步骤 1：编写动态路径失败测试**

固定测试函数名：

```go
func TestLayout_DynamicPathsPreserveSegments(t *testing.T)
func TestLayout_DynamicPathsRejectUnsafeSegments(t *testing.T)
func TestLayout_DynamicSegmentUTF16LengthBoundary(t *testing.T)
func TestAppendPartSuffix_UTF16LengthBoundary(t *testing.T)
func TestLayout_RuntimeLogFileUsesLocalDate(t *testing.T)
func TestLayout_RuntimeLogFileRejectsZeroDate(t *testing.T)
```

合法值测试精确断言：

```go
func TestLayout_DynamicPathsPreserveSegments(t *testing.T) {
    root := filepath.Join(t.TempDir(), "AUTO-MAS")
    layout, err := config.NewLayout(root, filepath.Dir(root))
    if err != nil {
        t.Fatal(err)
    }

    update, err := layout.RepoUpdateDir("Op-ID_1")
    if err != nil {
        t.Fatal(err)
    }
    if want := filepath.Join(root, "repo.update-Op-ID_1"); update != want {
        t.Fatalf("RepoUpdateDir() = %q, want %q", update, want)
    }

    previous, err := layout.RepoPreviousDir("Op-ID_1")
    if err != nil {
        t.Fatal(err)
    }
    if want := filepath.Join(root, "repo.previous-Op-ID_1"); previous != want {
        t.Fatalf("RepoPreviousDir() = %q, want %q", previous, want)
    }

    versionDir, err := layout.UVVersionDir("0.8.0")
    if err != nil {
        t.Fatal(err)
    }
    if want := filepath.Join(layout.UVToolsDir(), "0.8.0"); versionDir != want {
        t.Fatalf("UVVersionDir() = %q, want %q", versionDir, want)
    }

    executable, err := layout.UVExecutable("0.8.0")
    if err != nil {
        t.Fatal(err)
    }
    if want := filepath.Join(versionDir, "uv.exe"); executable != want {
        t.Fatalf("UVExecutable() = %q, want %q", executable, want)
    }

    download, err := layout.DownloadFile("uv-x86_64.zip")
    if err != nil {
        t.Fatal(err)
    }
    if want := filepath.Join(layout.DownloadCacheDir(), "uv-x86_64.zip"); download != want {
        t.Fatalf("DownloadFile() = %q, want %q", download, want)
    }

    part, err := layout.DownloadPartFile("uv-x86_64.zip")
    if err != nil {
        t.Fatal(err)
    }
    if want := filepath.Join(
        layout.DownloadCacheDir(),
        "uv-x86_64.zip.part",
    ); part != want {
        t.Fatalf("DownloadPartFile() = %q, want %q", part, want)
    }

    staging, err := layout.UVStagingDir("0.8.0", "Op-ID_1")
    if err != nil {
        t.Fatal(err)
    }
    if want := filepath.Join(
        layout.BuildCacheDir(),
        "uv",
        "0.8.0",
        "Op-ID_1",
    ); staging != want {
        t.Fatalf("UVStagingDir() = %q, want %q", staging, want)
    }
}
```

基础非法 segment 表固定包含：

```go
[]string{
    "", ".", "..", "a/b", `a\b`, "C:relative",
    "bad<name", "bad>name", `bad"name`, "bad|name", "bad?name",
    "bad*name", "bad:name", "trailing.", "trailing ",
    "CON", "con.txt", "PRN", "AUX.log", "NUL",
    "CONIN$", "conin$.txt", "CONOUT$", "conout$.log",
    "COM1", "COM9.bin", "LPT1", "lpt9.txt",
    strings.Repeat("a", 129),
    strings.Repeat("\U0001F600", 65),
}
```

测试初始化表后必须补入全部 ASCII 控制字符，不能只覆盖 NUL：

```go
for control := rune(0); control <= '\x1f'; control++ {
    unsafe = append(unsafe, "safe"+string(control)+"name")
}
```

每个值分别调用 `RepoUpdateDir`、`RepoPreviousDir`、`UVVersionDir`、
`UVExecutable`、`DownloadFile`、`DownloadPartFile` 和带非零日期的
`RuntimeLogFile`，断言 `errors.Is(err, config.ErrInvalidSegment)`，并断言失败时
路径为空。`UVStagingDir` 对同一张表执行两组独立子测试：

- `UVStagingDir(value, "Op-ID_1")`，覆盖非法 version 与合法 operation ID；
- `UVStagingDir("0.8.0", value)`，覆盖合法 version 与非法 operation ID。

两组均断言返回路径为空，且 `errors.Is(err, config.ErrInvalidSegment)` 为 true，
确保两个 segment 分别校验且任一失败都不泄漏部分路径。

`TestLayout_DynamicSegmentUTF16LengthBoundary` 使用表驱动用例固定区分 rune 数和
UTF-16 code unit 数：

```go
tests := []struct {
    name    string
    value   string
    wantErr bool
}{
    {"128 ASCII code units", strings.Repeat("a", 128), false},
    {"129 ASCII code units", strings.Repeat("a", 129), true},
    {"64 supplementary runes are 128 units", strings.Repeat("\U0001F600", 64), false},
    {"65 supplementary runes are 130 units", strings.Repeat("\U0001F600", 65), true},
}
```

每个用例调用 `DownloadFile`。合法边界必须保留原始文件名；非法边界必须返回
空路径并满足 `errors.Is(err, config.ErrInvalidSegment)`。非法表已把两个超限值
送入所有动态 getter，因此既证明公共边界，也证明所有方法都复用同一校验入口。

`segment_internal_test.go` 使用 `package config` 固定白盒测试
`TestAppendPartSuffix_UTF16LengthBoundary`。公共动态 segment 最多 128 code unit，
无法从黑盒输入触达 255 分支，因此该测试直接调用包内 `appendPartSuffix`：

```go
tests := []struct {
    name    string
    value   string
    want    string
    wantErr bool
}{
    {
        "250 ASCII plus suffix is 255 units",
        strings.Repeat("a", 250),
        strings.Repeat("a", 250) + ".part",
        false,
    },
    {
        "251 ASCII plus suffix is 256 units",
        strings.Repeat("a", 251),
        "",
        true,
    },
    {
        "125 supplementary runes plus suffix is 255 units",
        strings.Repeat("\U0001F600", 125),
        strings.Repeat("\U0001F600", 125) + ".part",
        false,
    },
    {
        "126 supplementary runes plus suffix is 257 units",
        strings.Repeat("\U0001F600", 126),
        "",
        true,
    },
}
```

失败用例必须满足 `errors.Is(err, ErrInvalidSegment)`；成功用例必须逐字保留输入并
只追加 `.part`。该 helper 只实施派生名 255 code unit 防御，不取代公共 128
code unit 校验。

本地日期测试使用：

```go
local := time.Date(2026, 7, 30, 0, 30, 0, 0, time.FixedZone("CST", 8*60*60))
got, err := layout.RuntimeLogFile("workspace-sync", local)
```

期望文件名为 `workspace-sync-20260730.log`；零值时间返回
`config.ErrInvalidLogDate`。

- [ ] **步骤 2：运行红灯并确认动态方法缺失**

```powershell
Invoke-ExpectedFailure `
    "Test(Layout_(DynamicPaths|DynamicSegment|RuntimeLogFile)|AppendPartSuffix)" `
    "(layout\.(RepoUpdateDir|DownloadFile|RuntimeLogFile) undefined|undefined: appendPartSuffix)"
```

- [ ] **步骤 3：实现统一 segment 校验**

`segment.go` 的核心实现：

```go
package config

import (
    "fmt"
    "path/filepath"
    "strings"
    "time"
    "unicode/utf16"
)

const (
    invalidSegmentCharacters     = `<>:"/\|?*`
    maxDynamicSegmentUTF16Units  = 128
    maxWindowsSegmentUTF16Units  = 255
)

func validateSegment(value string) error {
    if value == "" {
        return ErrInvalidSegment
    }
    for _, character := range value {
        if character <= '\x1f' {
            return ErrInvalidSegment
        }
    }
    if value == "." || value == ".." || filepath.VolumeName(value) != "" {
        return ErrInvalidSegment
    }
    if strings.ContainsAny(value, invalidSegmentCharacters) {
        return ErrInvalidSegment
    }
    if strings.HasSuffix(value, ".") || strings.HasSuffix(value, " ") {
        return ErrInvalidSegment
    }
    if isReservedDeviceName(value) {
        return ErrInvalidSegment
    }
    if utf16CodeUnits(value) > maxDynamicSegmentUTF16Units {
        return ErrInvalidSegment
    }
    return nil
}

func isReservedDeviceName(value string) bool {
    device := strings.ToUpper(strings.SplitN(value, ".", 2)[0])
    switch device {
    case "CON", "PRN", "AUX", "NUL", "CONIN$", "CONOUT$":
        return true
    }
    if len(device) != 4 || device[3] < '1' || device[3] > '9' {
        return false
    }
    return device[:3] == "COM" || device[:3] == "LPT"
}

func utf16CodeUnits(value string) int {
    return len(utf16.Encode([]rune(value)))
}

func appendPartSuffix(name string) (string, error) {
    derived := name + ".part"
    if utf16CodeUnits(derived) > maxWindowsSegmentUTF16Units {
        return "", ErrInvalidSegment
    }
    return derived, nil
}
```

实现不保存可变包级 map/slice。UTF-16 边界必须用 code unit 计数，不能使用
`len(value)` 字节数或 `utf8.RuneCountInString` rune 数。校验失败不得 trim、
替换字符或返回部分路径。

- [ ] **步骤 4：实现动态路径方法**

```go
func (l *Layout) RepoUpdateDir(operationID string) (string, error) {
    if err := validateSegment(operationID); err != nil {
        return "", fmt.Errorf("validate repository update operation id: %w", err)
    }
    return filepath.Join(l.appRoot, "repo.update-"+operationID), nil
}

func (l *Layout) RepoPreviousDir(operationID string) (string, error) {
    if err := validateSegment(operationID); err != nil {
        return "", fmt.Errorf("validate repository previous operation id: %w", err)
    }
    return filepath.Join(l.appRoot, "repo.previous-"+operationID), nil
}

func (l *Layout) UVVersionDir(version string) (string, error) {
    if err := validateSegment(version); err != nil {
        return "", fmt.Errorf("validate uv version: %w", err)
    }
    return filepath.Join(l.paths.uvToolsDir, version), nil
}

func (l *Layout) UVExecutable(version string) (string, error) {
    directory, err := l.UVVersionDir(version)
    if err != nil {
        return "", err
    }
    return filepath.Join(directory, "uv.exe"), nil
}

func (l *Layout) DownloadFile(name string) (string, error) {
    if err := validateSegment(name); err != nil {
        return "", fmt.Errorf("validate download file name: %w", err)
    }
    return filepath.Join(l.paths.downloadCacheDir, name), nil
}

func (l *Layout) DownloadPartFile(name string) (string, error) {
    if err := validateSegment(name); err != nil {
        return "", fmt.Errorf("validate download part file name: %w", err)
    }
    partName, err := appendPartSuffix(name)
    if err != nil {
        return "", fmt.Errorf("derive download part file name: %w", err)
    }
    return filepath.Join(l.paths.downloadCacheDir, partName), nil
}

func (l *Layout) UVStagingDir(version, operationID string) (string, error) {
    if err := validateSegment(version); err != nil {
        return "", fmt.Errorf("validate uv staging version: %w", err)
    }
    if err := validateSegment(operationID); err != nil {
        return "", fmt.Errorf("validate uv staging operation id: %w", err)
    }
    return filepath.Join(l.paths.buildCacheDir, "uv", version, operationID), nil
}

func (l *Layout) RuntimeLogFile(command string, localDate time.Time) (string, error) {
    if err := validateSegment(command); err != nil {
        return "", fmt.Errorf("validate log command: %w", err)
    }
    if localDate.IsZero() {
        return "", ErrInvalidLogDate
    }
    name := command + "-" + localDate.Format("20060102") + ".log"
    return filepath.Join(l.paths.runtimeLogDir, name), nil
}
```

- [ ] **步骤 5：验证绿灯并提交**

```powershell
Invoke-Checked {
    & $gofmt -w `
        "internal/config/segment.go" `
        "internal/config/segment_test.go" `
        "internal/config/segment_internal_test.go"
} "T2.1 Task 3 gofmt"
Invoke-PassingTest `
    "Test(Layout_(DynamicPaths|DynamicSegment|RuntimeLogFile)|AppendPartSuffix)"
Invoke-Checked { & $go test ./... -count=1 } "T2.1 Task 3 go test ./..."
Invoke-Checked { git diff --check } "T2.1 Task 3 git diff --check"

& git add -- `
    "internal/config/segment.go" `
    "internal/config/segment_test.go" `
    "internal/config/segment_internal_test.go"
if ($LASTEXITCODE -ne 0) { throw "T2.1 Task 3 staging failed" }
$staged = @(& git diff --cached --name-only)
if ($LASTEXITCODE -ne 0) { throw "T2.1 Task 3 staged list failed" }
$expected = @(
    "internal/config/segment.go",
    "internal/config/segment_test.go",
    "internal/config/segment_internal_test.go"
)
$delta = @(Compare-Object ($staged | Sort-Object) ($expected | Sort-Object))
if ($delta.Count -ne 0) { $delta; throw "T2.1 Task 3 staged unexpected files" }
& git commit -m "feat: 校验动态目录与下载日志文件名 (T2.1)"
if ($LASTEXITCODE -ne 0) { throw "T2.1 Task 3 commit failed" }
```

## 任务 4：保护名单、防御性副本与并发读取

**文件：**

- 创建：`internal/config/protection.go`
- 创建：`internal/config/protection_test.go`
- 修改：`internal/config/paths_test.go`

- [ ] **步骤 1：编写保护名单失败测试**

固定测试函数名：

```go
func TestLayout_ProtectedRootDirs(t *testing.T)
func TestLayout_ProtectedRootDirsReturnsDefensiveCopy(t *testing.T)
func TestLayout_GettersAreConcurrentSafe(t *testing.T)
func TestLayout_ConstructionAndGettersDoNotTouchFilesystem(t *testing.T)
```

保护名单测试必须按顺序断言：

```go
want := []string{
    layout.ConfigDir(),
    layout.DataDir(),
    layout.HistoryDir(),
    layout.ScriptDir(),
    layout.DebugDir(),
    layout.PluginsDir(),
    layout.LogsDir(),
}
if got := layout.ProtectedRootDirs(); !reflect.DeepEqual(got, want) {
    t.Fatalf("ProtectedRootDirs() = %#v, want %#v", got, want)
}
```

防御性副本测试修改第一次返回值的第一个元素并 append，再次调用必须仍等于 want。

并发测试使用关闭 channel 作为 barrier，32 个 goroutine 各循环 100 次调用全部固定
getter、七个动态 getter、`RuntimeLogFile`、`AppRoot`、`IdentityKey` 和
`ProtectedRootDirs`。错误通过
buffered channel 汇总，禁止 `time.Sleep`。固定函数形态：

```go
func TestLayout_GettersAreConcurrentSafe(t *testing.T) {
    layout, err := config.NewLayout(filepath.Join(t.TempDir(), "AUTO-MAS"), t.TempDir())
    if err != nil {
        t.Fatal(err)
    }

    start := make(chan struct{})
    errs := make(chan error, 32)
    var group sync.WaitGroup
    for worker := 0; worker < 32; worker++ {
        group.Add(1)
        go func() {
            defer group.Done()
            <-start
            for iteration := 0; iteration < 100; iteration++ {
                if _, err := layout.RepoUpdateDir("01JTEST"); err != nil {
                    errs <- err
                    return
                }
                if _, err := layout.RepoPreviousDir("01JTEST"); err != nil {
                    errs <- err
                    return
                }
                if _, err := layout.UVVersionDir("0.8.0"); err != nil {
                    errs <- err
                    return
                }
                if _, err := layout.UVExecutable("0.8.0"); err != nil {
                    errs <- err
                    return
                }
                if _, err := layout.DownloadFile("uv-x86_64.zip"); err != nil {
                    errs <- err
                    return
                }
                if _, err := layout.DownloadPartFile("uv-x86_64.zip"); err != nil {
                    errs <- err
                    return
                }
                if _, err := layout.UVStagingDir("0.8.0", "01JTEST"); err != nil {
                    errs <- err
                    return
                }
                if _, err := layout.RuntimeLogFile(
                    "workspace-sync",
                    time.Date(2026, 7, 29, 0, 0, 0, 0, time.UTC),
                ); err != nil {
                    errs <- err
                    return
                }
                _ = layout.ProtectedRootDirs()
                _ = layout.RuntimeLogDir()
                _ = layout.IdentityKey()
            }
        }()
    }
    close(start)
    group.Wait()
    close(errs)
    for err := range errs {
        t.Error(err)
    }
}
```

实现时补齐循环内全部固定 getter，不能只保留示例中的动态调用。

零副作用测试使用尚不存在的 app root，调用全部 getter 后仍断言 app root 不存在。

- [ ] **步骤 2：运行红灯并确认保护 API 缺失**

```powershell
Invoke-ExpectedFailure `
    "TestLayout_(ProtectedRootDirs|GettersAreConcurrentSafe|ConstructionAndGetters)" `
    "layout\.ProtectedRootDirs undefined"
```

- [ ] **步骤 3：实现保护名单**

`protection.go` 的完整生产实现：

```go
package config

func (l *Layout) ProtectedRootDirs() []string {
    return []string{
        l.paths.configDir,
        l.paths.dataDir,
        l.paths.historyDir,
        l.paths.scriptDir,
        l.paths.debugDir,
        l.paths.pluginsDir,
        l.paths.logsDir,
    }
}
```

不得保存或返回可变包级 slice；`runtime-state` 与 `runtime` 不加入绝对保护名单。

- [ ] **步骤 4：高重复验证并提交**

```powershell
Invoke-Checked {
    & $gofmt -w `
        "internal/config/protection.go" `
        "internal/config/protection_test.go" `
        "internal/config/paths_test.go"
} "T2.1 Task 4 gofmt"
Invoke-PassingTest `
    "TestLayout_(ProtectedRootDirs|GettersAreConcurrentSafe|ConstructionAndGetters)"
Invoke-Checked { & $go test ./internal/config -count=100 } "T2.1 config repeated tests"
Invoke-Checked { & $go test ./... -count=1 } "T2.1 Task 4 go test ./..."
Invoke-Checked { git diff --check } "T2.1 Task 4 git diff --check"

& git add -- `
    "internal/config/protection.go" `
    "internal/config/protection_test.go" `
    "internal/config/paths_test.go"
if ($LASTEXITCODE -ne 0) { throw "T2.1 Task 4 staging failed" }
$staged = @(& git diff --cached --name-only)
if ($LASTEXITCODE -ne 0) { throw "T2.1 Task 4 staged list failed" }
$expected = @(
    "internal/config/paths_test.go",
    "internal/config/protection.go",
    "internal/config/protection_test.go"
)
$delta = @(Compare-Object ($staged | Sort-Object) ($expected | Sort-Object))
if ($delta.Count -ne 0) { $delta; throw "T2.1 Task 4 staged unexpected files" }
& git commit -m "feat: 冻结根级保护目录 (T2.1)"
if ($LASTEXITCODE -ne 0) { throw "T2.1 Task 4 commit failed" }
```

## 任务 5：T2.1 全量门禁、独立审查与进度回写

**文件：**

- 修改：`doc/任务拆分.md`

- [ ] **步骤 1：运行格式、静态分析、构建与测试门禁**

```powershell
$env:GOCACHE = Join-Path $env:TEMP "auto-mas-runtime-t21-verify"

$unformatted = @(& $gofmt -l .)
if ($LASTEXITCODE -ne 0) { throw "T2.1 gofmt check failed" }
if ($unformatted.Count -gt 0) {
    $unformatted
    throw "T2.1 gofmt found unformatted files"
}

& $go vet ./...
if ($LASTEXITCODE -ne 0) { throw "T2.1 go vet failed" }
& $go build -buildvcs=false ./...
if ($LASTEXITCODE -ne 0) { throw "T2.1 go build failed" }
& $go test ./... -count=1
if ($LASTEXITCODE -ne 0) { throw "T2.1 go test failed" }
& $go test ./internal/config -count=100
if ($LASTEXITCODE -ne 0) { throw "T2.1 repeated config tests failed" }
& git diff --check
if ($LASTEXITCODE -ne 0) { throw "T2.1 git diff check failed" }
```

- [ ] **步骤 2：运行 race detector**

```powershell
$gccBin = Split-Path -Parent (Get-Command gcc -ErrorAction Stop).Source
$env:PATH = "$gccBin;$env:PATH"
$env:GOCACHE = Join-Path $env:TEMP "auto-mas-runtime-t21-race"
& $go test -race ./... -count=1
if ($LASTEXITCODE -ne 0) { throw "T2.1 race tests failed" }
```

若受管沙箱触发 `runtime/cgo` 工具链环境问题，按 `AGENTS.md` 第 5.3 节获批在沙箱外
重跑同一命令；只有完整退出码 0 可作为通过证据。

- [ ] **步骤 3：执行包边界扫描**

```powershell
$stdoutScan = @(& rg -n `
    'os\.Stdout|fmt\.F?Print|log\.Print|json\.NewEncoder' `
    --glob '*.go' `
    "internal/config" 2>&1)
$stdoutExit = $LASTEXITCODE
$stdoutScan
if ($stdoutExit -gt 1) { throw "T2.1 stdout scan failed" }
if ($stdoutExit -eq 0) { throw "T2.1 config contains a forbidden output path" }

$layoutScan = @(& rg -n `
    'repo\.update-|repo\.previous-|runtime-state|runtime[\\/]+cache[\\/]+(uv|downloads|build)|logs[\\/]+runtime|"\.part"|BuildCacheDir\(\)[^\r\n]*"uv"|DownloadCacheDir\(\)[^\r\n]*filepath\.Join' `
    --glob '*.go' `
    "internal" `
    "cmd" 2>&1)
$layoutExit = $LASTEXITCODE
$layoutScan
if ($layoutExit -gt 1) { throw "T2.1 layout ownership scan failed" }
$outsideConfig = @($layoutScan | Where-Object {
    $_ -notmatch '^internal[\\/]config[\\/]'
})
if ($outsideConfig.Count -gt 0) {
    throw "T2.1 found fixed layout construction outside internal/config"
}
```

`rg` 未匹配时退出码 1 属于成功结果；大于 1 才是工具失败。扫描只覆盖 Go 源码，不把
权威文档中的目录示例误报为实现违规。

- [ ] **步骤 4：独立规格与代码审查**

审查者逐项核对设计第 11 节的 17 项验收，并特别确认：

- NewLayout 不读取 cwd、环境变量、注册表或文件系统；
- app root 显示路径与 identity key 没有混用于 I/O；
- 22 个固定 getter、7 个动态 getter 和 `RuntimeLogFile` 与设计表一致；
- `DownloadFile`/`DownloadPartFile` 只从 download cache 拼接受校验名称；
- uv staging 精确采用 `BuildCacheDir()\uv\<version>\<operationID>` 层级；
- segment 拒绝 U+0000～U+001F、`CONIN$`/`CONOUT$` 和超过 128 UTF-16
  code unit 的输入，不会被 trim 或“清洗”成另一名称；
- `.part` 派生名在包内独立防御 255 UTF-16 code unit 上限；
- 七个保护目录顺序固定且返回防御性副本；
- 没有可变导出字段、可变包级 slice 或投机接口。

发现问题时先在上述固定测试函数中增加能复现的失败断言，再修复并重新执行步骤 1～3。

- [ ] **步骤 5：记录最新代码提交并回写 T2.1**

```powershell
$implementationCommit = (& git rev-parse --short HEAD).Trim()
if ($LASTEXITCODE -ne 0) { throw "T2.1 implementation hash lookup failed" }
if ($implementationCommit -notmatch '^[0-9a-f]+$') {
    throw "T2.1 implementation hash is invalid"
}
```

把任务标题更新为：

```markdown
- [x] **T2.1 config 与目录布局**（M） ✅ 2026-07-29 `<implementationCommit>`
```

里程碑 M2 尚有其他任务时保持未勾选。

- [ ] **步骤 6：只提交进度文档**

```powershell
& git add -- "doc/任务拆分.md"
if ($LASTEXITCODE -ne 0) { throw "T2.1 progress staging failed" }
$staged = @(& git diff --cached --name-only)
if ($LASTEXITCODE -ne 0) { throw "T2.1 progress staged list failed" }
if ($staged.Count -ne 1 -or $staged[0] -ne "doc/任务拆分.md") {
    $staged
    throw "T2.1 progress commit contains unexpected files"
}
& git commit -m "docs: 记录 T2.1 完成情况"
if ($LASTEXITCODE -ne 0) { throw "T2.1 progress commit failed" }
```

完成后不执行 push，也不提前合并或勾选 M2。
